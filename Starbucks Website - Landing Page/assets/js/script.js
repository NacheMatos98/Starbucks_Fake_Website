function imgSlider(anything){
    window.document.querySelector('.starbucks').src = anything;
}

function changeCircleColor(color){
    const circle = window.document.querySelector('.circle');
    circle.style.background = color;
}